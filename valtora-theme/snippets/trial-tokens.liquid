{%- comment -%}
  Resolve trial length in copy. Theme settings → Trial nights.
  Tokens: [N]  [N]-night  {{ settings.trial_nights }}
{%- endcomment -%}
{%- liquid
  assign trial = settings.return_window_days | default: settings.trial_nights | default: '30' | append: '' | strip
  if trial == blank
    assign trial = '30'
  endif
  if trial == '30'
    assign trial_phrase = '30-day'
  else
    assign trial_phrase = trial | append: '-night'
  endif
  assign out = text | default: ''
  assign out = out | replace: '{{ settings.trial_nights }}', trial
  assign out = out | replace: '[N]-night', trial_phrase
  assign out = out | replace: '[N]', trial
-%}
{{- out -}}

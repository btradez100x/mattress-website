# V9.1 checkpoint

Succeeded by V9.2.

V9 plus copy Part 2 (flip and construction). Two firmnesses via a flippable comfort module; nine-layer spec table; Stage A firmness line; new firmness FAQs. Tops are for replacement when worn, not for changing feel.

- `Valtora-Shopify-Theme-V9.1.zip` — upload to Shopify (Online Store → Themes → Add theme → Upload zip)
- `preview-and-theme.tar.gz` — full local rollback of `preview/` + `valtora-theme/`

Restore:
```bash
tar -xzf checkpoints/V9.1/preview-and-theme.tar.gz
```

Preview locally:
```bash
cd preview && python3 -m http.server 5173
# http://127.0.0.1:5173/
```

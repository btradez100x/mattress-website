# 13.0.0 — feel-gold-line

**Date:** Tuesday 8 Sep 2026  
Named deploy checkpoint. `VERSION` was not bumped.

| Tree | SHA |
|---|---|
| `v9` | `b9379e6` |
| `shopify-theme` | `2167359` |

## What shipped

The feel section gold line is centred under the centred eyebrow on the solo stack. Selector: `.feel-ladder__layout--solo .gold-rule` with `margin-left/right: auto`, and the same selectors on the brand.css last-wins centred-rule list so the 40px stub is not left-aligned. Split feel copy stays left. Gold lines under section titles still hide when `data-section-lines` is off.

## What was left out

No VERSION bump. Other section rules were not all centred. How it works copy, password 404, lifestyle images, press assets, and Checkout cream were not changed by this fix.

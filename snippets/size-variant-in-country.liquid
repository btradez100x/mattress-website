{%- comment -%}
  Always shown. Live Liquid was dropping US/AU/UAE sizes when Market Shown
  was blank or unreadable, so the picker no longer filters by country here.
  Every product.variant that Shopify publishes to the storefront is included.
  Expects: v, country_iso (unused; kept so existing renders stay valid)
{%- endcomment -%}
1

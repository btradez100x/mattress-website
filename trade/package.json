{
  "name": "onni-london-trade",
  "version": "0.0.0",
  "private": true,
  "description": "Onni London trade site. Static HTML, built with Node. No runtime dependencies.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "node build.mjs",
    "check": "node build.mjs --check",
    "test": "node --test --test-concurrency=1 test/*.test.mjs"
  }
}

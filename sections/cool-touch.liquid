{%- if section.settings.enable_section -%}
<section class="section{% if section.settings.surface %} section--surface{% endif %}" id="{{ section.settings.anchor_id | default: 'cool-touch' }}" data-reveal>
  <div class="page-width media-feature__split">
    {%- liquid
      assign main_caption = section.settings.image_caption
      if main_caption == blank
        assign main_caption = 'Cool-knit cover detail'
      endif
    -%}
    <figure class="cool-touch__main">
      <div class="media-feature__split-media">
        {%- if section.settings.video != blank -%}
          {{ section.settings.video | video_tag: image_size: '1400x', controls: true }}
        {%- else -%}
          {% render 'theme-image',
            image: section.settings.image,
            fallback: 'cover-macro.jpg',
            alt: main_caption,
            sizes: '(min-width: 900px) 50vw, 100vw'
          %}
        {%- endif -%}
      </div>
      <figcaption class="cool-touch__caption">{{ main_caption }}</figcaption>
    </figure>
    <div class="media-feature__split-copy">
      <p class="section__eyebrow">{{ section.settings.eyebrow }}</p>
      <hr class="gold-rule">
      <h2>{{ section.settings.heading }}</h2>
      {%- if section.settings.body != blank -%}
        <p class="section__lede">{{ section.settings.body }}</p>
      {%- endif -%}

      {%- if section.blocks.size > 0 -%}
        <ul class="cool-touch__points" role="list">
          {%- for block in section.blocks -%}
            {%- if block.type == 'point' and block.settings.enabled -%}
              <li {{ block.shopify_attributes }}>
                <strong>{{ block.settings.title }}</strong>
                {%- if block.settings.text != blank -%}
                  <span>{{ block.settings.text }}</span>
                {%- endif -%}
              </li>
            {%- endif -%}
          {%- endfor -%}
        </ul>
      {%- endif -%}

      {%- if section.settings.gallery_enabled -%}
        <div class="cool-touch__gallery">
          {%- for block in section.blocks -%}
            {%- if block.type == 'media' and block.settings.enabled -%}
              <figure class="cool-touch__thumb" {{ block.shopify_attributes }}>
                {%- liquid
                  assign media_fallback = block.settings.fallback_asset | default: 'cover-macro.jpg'
                  assign media_caption = block.settings.caption
                  if media_caption == blank
                    assign media_caption = 'Cover texture'
                  endif
                -%}
                <div class="cool-touch__thumb-frame">
                {%- if block.settings.video != blank -%}
                  {{ block.settings.video | video_tag: image_size: '600x', controls: true }}
                {%- else -%}
                  {% render 'theme-image',
                    image: block.settings.image,
                    fallback: media_fallback,
                    alt: media_caption,
                    sizes: '30vw'
                  %}
                {%- endif -%}
                </div>
                <figcaption class="cool-touch__caption">{{ media_caption }}</figcaption>
              </figure>
            {%- endif -%}
          {%- endfor -%}
        </div>
      {%- endif -%}
    </div>
  </div>
</section>
{%- endif -%}

{% schema %}
{
  "name": "Cool touch",
  "settings": [
    {
      "type": "checkbox",
      "id": "enable_section",
      "label": "Show this section",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "surface",
      "label": "Stone surface background",
      "default": false
    },
    {
      "type": "checkbox",
      "id": "gallery_enabled",
      "label": "Show supporting media gallery",
      "default": true
    },
    {
      "type": "text",
      "id": "anchor_id",
      "label": "Anchor ID",
      "default": "cool-touch"
    },
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow",
      "default": "Cool touch"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Engineered to sleep cool."
    },
    {
      "type": "textarea",
      "id": "body",
      "label": "Body",
      "default": "A breathable cool-knit cover and gel-infused comfort layer - made for warm nights without overclaiming miracles."
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Main image"
    },
    {
      "type": "text",
      "id": "image_caption",
      "label": "Main image caption",
      "default": "Cool-knit cover detail"
    },
    {
      "type": "video",
      "id": "video",
      "label": "Main video (optional)"
    }
  ],
  "blocks": [
    {
      "type": "point",
      "name": "Point",
      "settings": [
        {
          "type": "checkbox",
          "id": "enabled",
          "label": "Show this point",
          "default": true
        },
        {
          "type": "text",
          "id": "title",
          "label": "Title",
          "default": "Cool-knit cover"
        },
        {
          "type": "text",
          "id": "text",
          "label": "Text",
          "default": "400gsm, breathable hand-feel"
        }
      ]
    },
    {
      "type": "media",
      "name": "Gallery media",
      "settings": [
        {
          "type": "checkbox",
          "id": "enabled",
          "label": "Show this media",
          "default": true
        },
        {
          "type": "text",
          "id": "caption",
          "label": "Caption",
          "default": "Cover texture"
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        },
        {
          "type": "video",
          "id": "video",
          "label": "Video (optional)"
        },
        {
          "type": "text",
          "id": "fallback_asset",
          "label": "Theme fallback asset",
          "default": "cover-macro.jpg"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Cool touch",
      "blocks": [
        {
          "type": "point",
          "settings": {
            "enabled": true,
            "title": "Cool-knit cover",
            "text": "400gsm, breathable hand-feel"
          }
        },
        {
          "type": "point",
          "settings": {
            "enabled": true,
            "title": "Gel-infused comfort",
            "text": "Designed for a cooler first touch"
          }
        },
        {
          "type": "media",
          "settings": {
            "enabled": true,
            "caption": "Cover knit",
            "fallback_asset": "cover-macro.jpg"
          }
        },
        {
          "type": "media",
          "settings": {
            "enabled": true,
            "caption": "Surface detail",
            "fallback_asset": "handle-detail.jpg"
          }
        }
      ]
    }
  ]
}
{% endschema %}
